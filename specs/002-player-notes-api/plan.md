# Implementation Plan: Player Notes API

**Branch**: `002-player-notes-api` | **Date**: 2026-04-01 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/002-player-notes-api/spec.md`

## Summary

REST microservice for managing per-session notes about players. Each note is scoped to an
authenticated session (JWT `sub`) and a target player (`uniqCode`). Core rules: one active note
per (author, player) pair; max 100 active notes per session; 30-day TTL with unlimited extensions;
note content is an arbitrary JSON object (≤ 10 KB). Built with Java 21 + Spring Boot 3.x,
Spring Security 6 JWT (HS256), Spring Data JPA, and a scheduled cleanup job.

## Technical Context

**Language/Version**: Java 21
**Primary Dependencies**: Spring Boot 3.x, Spring Security 6 (OAuth2 Resource Server / HS256),
  Spring Data JPA, Hibernate, Bean Validation (Jakarta), Jackson, Spring Boot Actuator,
  Nimbus JOSE+JWT (transitive via Spring Security)
**Storage**: H2 in-memory (dev/local), PostgreSQL (production); Flyway for prod migrations
**Testing**: None — explicitly out of scope per constitution
**Target Platform**: Linux server (JVM)
**Project Type**: REST web service
**Performance Goals**: All API operations respond within 1 second (SC-003)
**Constraints**: Max 100 active notes per session; max 10 KB per note (UTF-8); JWT locally
  validated; no rate limiting beyond the 100-note cap
**Scale/Scope**: Single-instance microservice for v1; no distributed state

## Constitution Check

*GATE: Evaluated pre-design and re-checked post-design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Java-First | ✅ PASS | Java 21 + Spring Boot 3.x; no other language introduced |
| II. REST API Design | ✅ PASS | Noun resources (`/notes`, `/notes/{id}`), HTTP verbs express intent, JSON bodies, contracts defined in `openapi.yaml` before implementation |
| III. Atomic Commits | ✅ PASS | One task = one commit; enforced by workflow |
| IV. Simplicity (YAGNI) | ✅ PASS | No repository pattern beyond Spring Data; no caching layer; no pagination; no soft-delete |
| V. Observability | ✅ PASS | SLF4J/Logback structured logging; `GET /actuator/health` via Spring Boot Actuator |

No violations. No Complexity Tracking entries required.

## Project Structure

### Documentation (this feature)

```text
specs/002-player-notes-api/
├── plan.md              ← this file
├── research.md          ← Phase 0 decisions
├── data-model.md        ← entity model + DDL
├── quickstart.md        ← developer guide
├── contracts/
│   └── openapi.yaml     ← REST API contract
└── tasks.md             ← generated by /speckit.tasks
```

### Source Code (repository root)

```text
src/
└── main/
    ├── java/com/example/playernotes/
    │   ├── PlayerNotesApplication.java
    │   ├── config/
    │   │   └── SecurityConfig.java
    │   ├── controller/
    │   │   └── NoteController.java
    │   ├── service/
    │   │   └── NoteService.java
    │   ├── repository/
    │   │   └── NoteRepository.java
    │   ├── entity/
    │   │   └── Note.java
    │   ├── dto/
    │   │   ├── CreateNoteRequest.java
    │   │   ├── UpdateNoteRequest.java
    │   │   └── NoteResponse.java
    │   ├── scheduler/
    │   │   └── ExpiredNoteCleanupJob.java
    │   └── exception/
    │       ├── GlobalExceptionHandler.java
    │       ├── NoteNotFoundException.java
    │       ├── DuplicateNoteException.java
    │       └── NoteLimitExceededException.java
    └── resources/
        ├── application.yml
        ├── application-prod.yml
        └── db/migration/
            └── V1__create_notes_table.sql

pom.xml
```

**Structure Decision**: Single Maven project at repository root. Standard Spring Boot
layout with packages by layer (`config`, `controller`, `service`, `repository`, `entity`,
`dto`, `scheduler`, `exception`). No separate backend/frontend split needed.

## Design Decisions

### Entity Shape
- `id`: `Long`, auto-generated by DB (`BIGSERIAL`).
- `userId`: `Long`, parsed from JWT `sub` claim via `Long.parseLong(jwt.getSubject())`.
- `uniqCode`: `String`, non-blank, immutable after creation.
- `data`: stored as `JSONB` in PostgreSQL; mapped as `String` in JPA with
  `@Column(columnDefinition = "jsonb")`. H2 (dev) uses `TEXT`.
- `noteTimestamp`: `Long`, Unix seconds at creation (`Instant.now().getEpochSecond()`).
- `noteEOLTimestamp`: `Long`, Unix seconds = `noteTimestamp + 2592000` on creation;
  reset to `Instant.now().getEpochSecond() + 2592000` on extend.

### Authentication
- Spring Security 6 OAuth2 Resource Server with HS256 JWT.
- `JwtDecoder` bean via `NimbusJwtDecoder.withSecretKey()` — declared explicitly in `SecurityConfig`.
- `userId` = `Long.parseLong(jwt.getSubject())` from `@AuthenticationPrincipal Jwt`.
- Secret injected via `APP_SECURITY_JWT_SECRET` environment variable (Base64, ≥ 256 bits).

### Data Validation
- Size check: `data.getBytes(StandardCharsets.UTF_8).length <= 10_240` — before JSON parse.
- JSON object check: `objectMapper.readTree(data).isObject()` — rejects arrays, primitives,
  null, malformed JSON.
- Both checks in `NoteService` before any persistence call.

### Uniqueness Enforcement
- Primary guard: `noteRepository.existsByUserIdAndUniqCodeAndNoteEOLTimestampAfter(userId, uniqCode, nowSeconds)` → HTTP 409.
- No DB-level partial unique index (PostgreSQL does not support volatile predicates).
  DB integrity violations caught and mapped to 409 as fallback.

### Note Limit
- Check: `noteRepository.countByUserIdAndNoteEOLTimestampAfter(userId, nowSeconds) >= 100` → HTTP 422.
- Checked before uniqueness check (limit is a harder gate).

### Expiry Lifecycle
- `noteEOLTimestamp` = `noteTimestamp + 2592000` on creation.
- `noteEOLTimestamp` = `now + 2592000` on extend — always reset from current moment.
- All queries filter `note_eol_timestamp > nowSeconds` — expired notes invisible to API.
- Background job (`ExpiredNoteCleanupJob`) runs daily at 02:00 via `@Scheduled(cron)`;
  deletes rows where `note_eol_timestamp < nowSeconds`.

### List Response
- Ordered `ORDER BY note_timestamp DESC` at query level.

### Error Responses
- `GlobalExceptionHandler` (`@RestControllerAdvice`) maps all custom exceptions and
  validation errors to `ErrorResponse` with `status`, `error`, `message`, `timestamp`.

### DB Index
```sql
CREATE INDEX idx_notes_user_active
    ON notes (user_id, note_eol_timestamp DESC, note_timestamp DESC);
```
Supports: active-note list, count check, uniqueness check — all in one composite index.
